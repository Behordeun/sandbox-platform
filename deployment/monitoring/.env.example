# Monitoring stack - Example env

# Grafana admin credentials
GF_SECURITY_ADMIN_PASSWORD=change-me

# Read-only DB user for Grafana dashboards (optional)
GRAFANA_RO_USER=readonly
GRAFANA_RO_PASSWORD=change-me

# Postgres connection (if Grafana datasource uses Postgres)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me
POSTGRES_DB=sandbox_platform
