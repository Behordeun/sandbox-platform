apiVersion: v1
kind: Service
metadata:
  name: {{ include "nin.fullname" . }}
  labels:
    {{- include "nin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "nin.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}

