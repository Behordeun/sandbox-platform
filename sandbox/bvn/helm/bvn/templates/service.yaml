apiVersion: v1
kind: Service
metadata:
  name: {{ include "bvn.fullname" . }}
  labels:
    {{- include "bvn.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "bvn.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
